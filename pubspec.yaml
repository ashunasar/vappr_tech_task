name: vappr_tech_task
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.2
  get: 4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 2.4.4
  flutter_gen_runner: 5.3.1

  flutter_lints: 2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
